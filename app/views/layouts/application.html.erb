<!DOCTYPE html>
<html>
<head>
	<title><%= @title or Omegrep %></title>
	<%= stylesheet_link_tag "application", :media => "all" %>
	<%= csrf_meta_tags %>
</head>
<body>
	<%= content_for?(:content) ? yield(:content) : yield %>

	<%= javascript_include_tag "application" %>
</body>
</html>
